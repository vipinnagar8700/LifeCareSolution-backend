import { NativeOAuthBridgeContract } from './NativeOAuthBridgeContract';
import { ExternalConstants } from '../../constants/PhonePeSDKExternalConstants';
import Permission = ExternalConstants.Permission;
export declare class CommonNativeOAuthBridge implements NativeOAuthBridgeContract {
    fetchGrantToken(): Promise<any>;
    resourceGrant(permissions: Array<Permission>): Promise<any>;
    fetchAuthToken(): Promise<any>;
}
