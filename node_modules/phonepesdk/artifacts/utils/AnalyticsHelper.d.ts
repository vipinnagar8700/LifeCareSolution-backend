import { ExternalConstants } from '../constants/PhonePeSDKExternalConstants';
import Species = ExternalConstants.Species;
export declare class AnalyticsBridgeUtil {
    static getEventLoggerForBridge(species: Species, operatingSystem: ExternalConstants.OS, eventType: string): any;
}
