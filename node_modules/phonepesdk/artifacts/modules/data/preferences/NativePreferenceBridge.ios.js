"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
class IOSNativePreferenceBridge {
    constructor(preferenceBridge) {
        this.preferenceBridge = preferenceBridge;
    }
    getString(preferenceName, key, defaultValue) {
        return this.preferenceBridge.get(preferenceName, key, defaultValue);
    }
    getNumber(preferenceName, key, defaultValue) {
        return this.preferenceBridge.get(preferenceName, key, defaultValue);
    }
    getDecryptedUserId() {
        return this.preferenceBridge.getDecryptedUserId();
    }
    saveNumber(preferenceName, key, value) {
        this.preferenceBridge.set(preferenceName, key, value);
    }
    saveString(preferenceName, key, value) {
        this.preferenceBridge.set(preferenceName, key, value);
    }
    removeItem(preferenceName, key) {
        this.preferenceBridge.remove(preferenceName, key);
    }
}
exports.IOSNativePreferenceBridge = IOSNativePreferenceBridge;
