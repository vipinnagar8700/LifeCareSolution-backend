import { NativePermissionsBridgeContract } from './NativePermissionsBridgeContract';
import { ExternalConstants } from './../../constants/PhonePeSDKExternalConstants';
import OS = ExternalConstants.OS;
export declare class ReactNativePermissionsBridge implements NativePermissionsBridgeContract {
    operatingSystem: OS;
    constructor(operatingSystem: OS);
    seekPermission(listOfPermissions: ExternalConstants.Permission[]): Promise<any>;
    openSettingsPageForPermission(): Promise<any>;
}
