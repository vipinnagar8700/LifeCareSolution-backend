import { NativeFilePickerBridgeContract } from './NativeFilePickerBridgeContract';
export declare class WebNativeFilePickerBridge implements NativeFilePickerBridgeContract {
    selectFile(mimeType: string, allowMultiple: boolean): Promise<any>;
    readFile(uri: string, offset: number, length: number): Promise<any>;
}
